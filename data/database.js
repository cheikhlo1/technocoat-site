const day = (n) => `2026-05-${String(((n - 1) % 28) + 1).padStart(2, '0')}`;

export const initialDatabase = {
  clients: Array.from({ length: 5 }, (_, i) => ({
    id: i + 1,
    nom: `Client ${i + 1}`,
    secteur: ['Aéronautique', 'Automobile', 'Énergie', 'BTP', 'Ferroviaire'][i % 5],
    contactPrincipal: `Contact ${i + 1}`,
    email: `client${i + 1}@example.com`,
    telephone: `+33 1 40 00 0${i + 1}0`,
    adresse: `${10 + i} rue Industrielle, 7500${i}`,
    statut: i % 2 ? 'Actif' : 'Prospect',
    commentaire: `Compte client ${i + 1}`
  })),
  commandes: Array.from({ length: 8 }, (_, i) => ({
    id: i + 1,
    numeroCommande: `CMD-2026-${String(i + 1).padStart(3, '0')}`,
    clientId: (i % 5) + 1,
    dateCommande: day(i + 1),
    dateLivraisonDemandee: day(i + 8),
    montant: 5000 + i * 850,
    priorite: ['Basse', 'Moyenne', 'Haute'][i % 3],
    statut: ['Planifiée', 'En cours', 'Validée'][i % 3],
    commentaire: `Commande plan de charge ${i + 1}`
  })),
  affaires: Array.from({ length: 8 }, (_, i) => ({
    id: i + 1,
    numeroOF: `OF-2026-${String(i + 1).padStart(3, '0')}`,
    commandeId: (i % 8) + 1,
    clientId: (i % 5) + 1,
    dateReceptionPieces: day(i + 2),
    dateProductionPrevue: day(i + 5),
    dateLivraisonDemandee: day(i + 12),
    priorite: ['Basse', 'Moyenne', 'Haute'][i % 3],
    statutGlobal: ['Ouverte', 'En production', 'Terminée'][i % 3],
    localisationActuelle: ['Préparation', 'Accroche', 'Peinture', 'Qualité'][i % 4],
    avancementPourcentage: Math.min(100, 10 + i * 12),
    commentaire: `Affaire de production ${i + 1}`
  })),
  referencesPieces: Array.from({ length: 15 }, (_, i) => ({
    id: i + 1,
    affaireId: (i % 8) + 1,
    referencePiece: `REF-P-${String(i + 1).padStart(4, '0')}`,
    designationPiece: `Pièce ${i + 1}`,
    quantite: 20 + i,
    matiere: ['Acier', 'Aluminium', 'Inox'][i % 3],
    traitement: ['Thermolaquage', 'Anodisation', 'Cataphorèse'][i % 3],
    couleur: ['Noir', 'Bleu', 'Gris', 'Rouge'][i % 4],
    gammeId: (i % 5) + 1,
    statutReference: ['À lancer', 'En cours', 'Terminée'][i % 3],
    localisationActuelle: ['Stock entrée', 'Ligne', 'Contrôle'][i % 3],
    commentaire: `Référence client ${i + 1}`
  })),
  gammes: Array.from({ length: 5 }, (_, i) => ({
    id: i + 1,
    nomGamme: `Gamme ${i + 1}`,
    referenceType: `TYPE-${i + 1}`,
    description: `Standard process ${i + 1}`,
    tempsPrevuTotal: 120 + i * 15,
    nombreEtapes: 5,
    version: `v1.${i}`,
    statutGamme: i % 2 ? 'Active' : 'En revue',
    commentaireMethode: `Validation méthode ${i + 1}`
  })),
  etapesGamme: Array.from({ length: 25 }, (_, i) => ({
    id: i + 1,
    gammeId: (i % 5) + 1,
    nomEtape: ['Préparation', 'Accroche', 'Peinture', 'Décroche', 'Qualité'][i % 5],
    ordreEtape: (i % 5) + 1,
    activite: ['Préparation', 'Accroche', 'Peinture', 'Décroche', 'Qualité'][i % 5],
    tempsPrevu: 20 + (i % 5) * 10,
    posteConcerne: `Poste-${(i % 6) + 1}`,
    controleNecessaire: i % 2 ? 'Oui' : 'Non',
    consommablesNecessaires: ['Crochets', 'Masquage', 'Poudre'][i % 3],
    outillageNecessaire: ['Rack', 'Pistolet', 'Jauge'][i % 3],
    pointVigilance: `Vigilance étape ${i + 1}`
  })),
  taches: Array.from({ length: 30 }, (_, i) => ({
    id: i + 1,
    affaireId: (i % 8) + 1,
    referenceId: (i % 15) + 1,
    etapeId: (i % 25) + 1,
    operateurId: (i % 10) + 1,
    activite: ['Préparation', 'Accroche', 'Peinture', 'Décroche', 'Qualité'][i % 5],
    datePrevue: day(i + 1),
    heureDebutPrevue: '08:00',
    heureFinPrevue: '10:00',
    tempsPrevu: 120,
    heureDebutReelle: i % 3 ? '08:10' : '',
    heureFinReelle: i % 3 ? '10:05' : '',
    tempsReel: i % 3 ? 115 : 0,
    quantiteTraitee: 10 + (i % 6),
    quantiteBloquee: i % 7 ? 0 : 1,
    statutTache: ['Planifiée', 'En cours', 'Terminée'][i % 3],
    priorite: ['Basse', 'Moyenne', 'Haute'][i % 3],
    commentaireOperateur: `Retour opérateur ${i + 1}`
  })),
  personnel: Array.from({ length: 10 }, (_, i) => ({
    id: i + 1,
    nom: `Nom${i + 1}`,
    prenom: `Prenom${i + 1}`,
    login: `user${i + 1}`,
    email: `user${i + 1}@technocoat.local`,
    role: ['Manager', 'Responsable méthode', 'Préparation', 'Accroche', 'Peinture', 'Décroche', 'Qualité', 'Logistique', 'Administrateur', 'Stock'][i % 10],
    activite: ['Préparation', 'Accroche', 'Peinture', 'Décroche', 'Qualité'][i % 5],
    niveauAcces: ['Standard', 'Avancé', 'Admin'][i % 3],
    statutCompte: i % 2 ? 'Actif' : 'Inactif',
    derniereConnexion: `2026-05-${String((i % 28) + 1).padStart(2, '0')} 08:30`,
    commentaire: `Compte utilisateur ${i + 1}`
  })),
  stocks: Array.from({ length: 20 }, (_, i) => ({
    id: i + 1,
    codeArticle: `STK-${String(i + 1).padStart(4, '0')}`,
    designation: `Article ${i + 1}`,
    famille: ['Peinture', 'Consommable', 'Outillage', 'Quincaillerie'][i % 4],
    stockActuel: 100 - i,
    seuilMinimum: 20,
    unite: 'pcs',
    emplacement: `Zone-${(i % 5) + 1}`,
    fournisseur: `Fournisseur ${((i % 6) + 1)}`,
    statutStock: i % 4 ? 'Disponible' : 'Alerte',
    commentaire: `Suivi stock ${i + 1}`
  })),
  mouvementsStock: Array.from({ length: 15 }, (_, i) => ({
    id: i + 1,
    articleId: (i % 20) + 1,
    dateMouvement: day(i + 3),
    typeMouvement: ['Entrée', 'Sortie', 'Correction'][i % 3],
    quantite: 5 + i,
    stockAvant: 80 + i,
    stockApres: 85 + i,
    utilisateur: `user${((i % 10) + 1)}`,
    affaireId: (i % 8) + 1,
    commentaire: `Mouvement stock ${i + 1}`
  })),
  qualitePieces: Array.from({ length: 10 }, (_, i) => ({
    id: i + 1,
    affaireId: (i % 8) + 1,
    referenceId: (i % 15) + 1,
    dateControle: day(i + 4),
    typeControle: ['Visuel', 'Dimensionnel', 'Adhérence'][i % 3],
    controleur: `Q${(i % 4) + 1}`,
    resultat: i % 4 ? 'Conforme' : 'Non conforme',
    quantiteControlee: 20,
    quantiteConforme: i % 4 ? 20 : 17,
    quantiteNonConforme: i % 4 ? 0 : 3,
    decision: i % 4 ? 'Libérée' : 'Retouche',
    commentaire: `Contrôle qualité ${i + 1}`
  })),
  controlesEquipements: Array.from({ length: 8 }, (_, i) => ({
    id: i + 1,
    dateControle: day(i + 2),
    equipement: `EQ-${String(i + 1).padStart(3, '0')}`,
    typeControle: ['pH', 'Température', 'Conductivité'][i % 3],
    valeurMesuree: 7.1 + i * 0.1,
    unite: i % 3 ? '°C' : 'pH',
    valeurCible: i % 3 ? 25 : 7,
    tolerance: i % 3 ? '±2' : '±0.3',
    resultat: i % 3 ? 'Conforme' : 'Alerte',
    controleur: `Tech ${((i % 3) + 1)}`,
    actionCorrective: i % 3 ? 'Aucune' : 'Ajustement bain',
    commentaire: `Contrôle équipement ${i + 1}`
  })),
  logistique: Array.from({ length: 16 }, (_, i) => ({
    id: i + 1,
    affaireId: (i % 8) + 1,
    clientId: (i % 5) + 1,
    referenceId: (i % 15) + 1,
    dateMouvement: day(i + 4),
    typeMouvement: [
      'Réception prévue',
      'Réception pièces brutes',
      'Mise à disposition atelier',
      'Conditionnement',
      'Mise en palette',
      'Expédition client',
      'Réception matière',
      'Mise à disposition produits finis'
    ][i % 8],
    quantite: 12 + i,
    quantiteAttendue: 14 + i,
    quantiteRecue: 10 + i,
    quantiteExpediee: 8 + i,
    statut: ['Prête expédition', 'Préparée', 'Réception terminée', 'Retardée'][i % 4],
    statutPalette: ['À préparer', 'En cours', 'Terminée', 'Bloquée'][i % 4],
    typeConditionnement: ['Bac métal', 'Caisse bois', 'Filmage', 'Carton renforcé'][i % 4],
    numeroPalette: `PAL-2026-${String(i + 1).padStart(3, '0')}`,
    nombreColis: (i % 5) + 1,
    numeroBL: `BL-2026-${String(i + 1).padStart(3, '0')}`,
    transporteur: `Transporteur ${((i % 4) + 1)}`,
    emplacementDepose: `Zone-${(i % 4) + 1}`,
    emplacementPalette: `Palette-${(i % 6) + 1}`,
    caristeAffecte: `Cariste ${((i % 3) + 1)}`,
    actionAttendue: ['Contrôle réception', 'Transfert atelier', 'Finaliser palette', 'Préparer BL'][i % 4],
    commentaire: `Flux logistique ${i + 1}`
  })),
  observations: Array.from({ length: 15 }, (_, i) => ({
    id: i + 1,
    dateObservation: day(i + 1),
    operateurId: (i % 10) + 1,
    affaireId: (i % 8) + 1,
    referenceId: (i % 15) + 1,
    activite: ['Préparation', 'Accroche', 'Peinture', 'Décroche', 'Qualité'][i % 5],
    typeObservation: ['Anomalie', 'Suggestion', 'Sécurité'][i % 3],
    importance: ['Faible', 'Moyenne', 'Critique'][i % 3],
    commentaire: `Observation terrain ${i + 1}`,
    statutTraitement: ['Ouverte', 'En cours', 'Clôturée'][i % 3],
    actionPrevue: `Action ${i + 1}`,
    responsableTraitement: `Responsable ${((i % 5) + 1)}`,
    dateCloture: i % 3 === 2 ? day(i + 10) : ''
  }))
};
